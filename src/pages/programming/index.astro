---
import ProgrammingLayout from "../../layouts/ProgrammingLayout.astro";
import { getCollection } from "astro:content";

const projects = await getCollection("programming");
const sorted = projects.sort(
  (a, b) => b.data.date.getTime() - a.data.date.getTime(),
);
---

<ProgrammingLayout>
  <section class="py-12">
    <!-- <div class="grid gap-6">
      {
        sorted.map((project) => (
          <article
            class="p-6 rounded-xl border transition-all duration-200 hover:border-white/20"
            style={{
              backgroundColor: "var(--color-muted)",
              borderColor: "var(--color-border)",
            }}
          >
            <div class="flex items-start justify-between gap-4">
              <div>
                <h2
                  class="text-xl font-semibold mb-2"
                  style="font-family: var(--font-heading);"
                >
                  {project.data.title}
                </h2>
                <p class="mb-3" style="color: var(--color-accent);">
                  {project.data.description}
                </p>
                <div class="flex flex-wrap gap-2">
                  {project.data.tags.map((tag) => (
                    <span
                      class="text-xs px-2 py-1 rounded-full"
                      style={{
                        backgroundColor: "var(--color-border)",
                        color: "var(--color-accent)",
                      }}
                    >
                      {tag}
                    </span>
                  ))}
                </div>
              </div>
              <div class="flex gap-3">
                {project.data.repo && (
                  <a
                    href={project.data.repo}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="text-sm underline"
                    style="color: var(--color-accent);"
                  >
                    Repo
                  </a>
                )}
                {project.data.url && (
                  <a
                    href={project.data.url}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="text-sm underline"
                    style="color: var(--color-accent);"
                  >
                    Live
                  </a>
                )}
              </div>
            </div>
          </article>
        ))
      }
    </div> -->
  </section>
</ProgrammingLayout>
