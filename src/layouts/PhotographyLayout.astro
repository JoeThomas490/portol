---
import BaseLayout from "./BaseLayout.astro";

interface Props {
  title?: string;
}

const { title = "Photography" } = Astro.props;
---

<BaseLayout title={title} mode="photography">
  <div class="w-full h-[calc(100vh-4rem)] relative overflow-hidden">
    <slot />
  </div>
</BaseLayout>
